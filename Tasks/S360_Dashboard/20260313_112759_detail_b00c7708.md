# S360 Item Detail - ASP Bleu Onboarding for SignalRService_Triage

**KPI ID**: `b00c7708-0bb0-4b4d-bb2a-167e8e724ea1`
**Service Target ID**: `624c481d-e51c-4016-a522-fbe180d125fc`
**Service**: Azure SignalR Service
**Scope Used For This Report**: Azure SignalR Service only
**SLA Status**: `ApproachingSla` (1 item)
**Due Date**: 2026-03-31
**Exceptions**: 0
**Clouds**: Public
**Type**: Type E - Workflow / Process Deliverable
**Assigned To**: `kenchen`

## Summary

This KPI is a concrete ASP Bleu cloud onboarding task for Azure SignalR Service, not a resource remediation item. The service team must onboard `SignalRService_Triage` to ASP Bleu, then complete deployment, validation, and work item closure before 2026-03-31.

## What This Task Is

S360 metadata describes `ASP Action Items` in general terms, but the linked ASP document makes the specific intent clear for this item. The task behind work item `37023320` is to onboard the `SignalRService_Triage` supportability workflow into ASP's Bleu cloud environment.

In practice, this means ensuring that the triage diagnostics path used by Azure SignalR Service can run correctly in Bleu. Depending on how `SignalRService_Triage` is implemented, that can include provider configuration, data-source allowlisting, Key Vault allowlisting, ACIS endpoint updates, declarative diagnostics updates, SDK version alignment, deployment, and validation.

The work is therefore a supportability onboarding and readiness task, not a fix on a specific ARM resource.

## Evidence Reviewed

- **S360 active item payload**
  Confirmed there is exactly one active Azure SignalR Service item for this KPI, assigned to `kenchen`, due on `2026-03-31`, with URL `https://msazure.visualstudio.com/One/_workitems/edit/37023320`.
- **S360 KPI metadata**
  Confirmed the KPI display name is `ASP Action Items`, support owner is `Azure Diagnostic Services` / `Azure Solutions Team`, and the generic KPI covers supportability issues such as missing CRC articles, diagnostics reliability, missing replacement text, and over-broad targeting.
- **ASP Bleu onboarding document**
  Successfully opened the linked ASP support document via browser inspection and extracted the full Bleu onboarding checklist.
- **ADO work item page**
  Browser navigation to work item `37023320` redirected to Azure DevOps sign-in. The item link is confirmed, but comments, acceptance criteria, and linked tasks were not readable in the current session.

## Service-Specific Mapping

The object being onboarded is `SignalRService_Triage`, which is a supportability / diagnostics workflow for Azure SignalR Service rather than an ARM resource. For this service, the task likely maps to one or more of the following assets:

- triage provider configuration
- triage data-source access configuration
- triage Key Vault or secret access configuration
- triage ACIS operations or extensions
- triage declarative diagnostics definitions
- ASP partner SDK usage in the triage microservice
- Bleu deployment and post-deployment validation flow

The exact subset depends on how `SignalRService_Triage` is currently implemented.

## Affected Objects

| Object Type | Object | Evidence |
|-------------|--------|----------|
| Workflow / supportability scenario | `SignalRService_Triage` | S360 item title |
| Tracking work item | `37023320` | S360 item URL |
| Cloud onboarding target | `ASP Bleu cloud` | ASP onboarding document |
| Service mapping | `624c481d-e51c-4016-a522-fbe180d125fc` | S360 target scope |

## Concrete Work Items

The ASP onboarding guide defines the following checklist. Steps 8, 9, and 10 are effectively mandatory. Steps 1 through 6 are conditional, depending on the implementation details of `SignalRService_Triage`.

### Step 1: Update Bleu provider configuration

Apply this step if `SignalRService_Triage` owns provider configuration or uses partner provider config override.

Required action:
- create or update the Bleu provider config file, such as `ProviderConfig_Bleu_Prod.json`
- merge the configuration change
- validate that the Bleu provider call succeeds

### Step 2: Allowlist ASP Bleu identities on partner data sources

Apply this step if the triage flow accesses service-owned data sources or uses ASP-owned providers such as Kusto, ACIS, MDM, or DGrep.

Required action:
- allowlist the ASP Bleu identity on the relevant data store
- verify the data source can be accessed successfully in Bleu

### Step 3: Allowlist ASP app on partner secret storage

Apply this step if the triage logic reads secrets or certificates from a service-owned Key Vault.

Required action:
- grant the ASP app the required access to the Key Vault or secret store
- verify secret retrieval works in Bleu

### Step 4: Update ACIS operations or ACIS extensions for Bleu

Apply this step if the triage flow uses `ACISProvider`, owns ACIS operations, or defines ACIS extensions.

Required action:
- add a `BleuEndpoint` entry for partner-owned ACIS operations
- if custom ACIS claims are required, follow the ASP guidance to allowlist those claims
- merge and validate the ACIS changes

### Step 5: Update declarative diagnostics definitions for Bleu

Apply this step if the service uses express diagnostics and has `.dd` files.

Required action:
- inspect declarative diagnostics definitions for ACIS endpoint configuration
- add the corresponding `BleuEndpoint` entries where needed
- merge the definition updates

### Step 6: Update to the latest ASPPartner SDK

Apply this step if `SignalRService_Triage` is not already on the latest ASPPartner SDK version.

Required action:
- inspect the current `ASPPartnerVersion`
- update to the latest supported version if needed
- merge the SDK version change

### Step 7: Optional cloud tests onboarding

This step is optional but recommended.

Required action:
- onboard partner cloud tests, configs, and test data into ASP partner cloud tests if the team wants automated validation for future deployments

### Step 8: Complete Bleu deployment

Required action:
- perform the normal deployment flow after any required code or config updates
- ensure deployment succeeds through Bleu regions

### Step 9: Complete Bleu validation

Required action:
- validate diagnostics through ACIS actions in Bleu
- ensure the diagnostic behaves correctly in Bleu
- allow ASP reliability verification to pass

### Step 10: Finish work item and close S360

Required action:
- mark work item `37023320` done once all required onboarding, deployment, and validation steps are complete
- ensure S360 closes without going out of SLA

## Prioritized Execution Order

For Azure SignalR Service, the practical execution order should be:

1. Determine whether `SignalRService_Triage` uses provider configs, service-owned data stores, Key Vault, ACIS operations, `.dd` definitions, or an outdated ASPPartner SDK.
2. Perform whichever of Steps 1 through 6 are actually applicable.
3. Complete Bleu deployment.
4. Complete Bleu validation.
5. Close the ADO work item and confirm S360 is satisfied.

## What Is Already Certain vs Still Conditional

### Already Certain

- This is a Bleu onboarding task for `SignalRService_Triage`.
- It is not a direct ARM resource remediation item.
- Deployment and validation in Bleu are required.
- Work item `37023320` is the authoritative tracker.
- The due date is 2026-03-31.

### Still Conditional

The following depend on the implementation details of `SignalRService_Triage`:

- whether provider config changes are needed
- whether data-source allowlisting is needed
- whether Key Vault allowlisting is needed
- whether ACIS operation or ACIS extension changes are needed
- whether declarative diagnostics `.dd` updates are needed
- whether the ASPPartner SDK needs an update

## Open Questions / Ambiguities

- Which of Steps 1 through 6 actually apply to `SignalRService_Triage` for Azure SignalR Service.
- Whether work item `37023320` contains comments, linked tasks, or acceptance criteria that narrow the required subset of steps.
- Whether `SignalRService_Triage` already has partial Bleu support implemented, reducing the remaining work.

## Blockers

- **Source**: `https://msazure.visualstudio.com/One/_workitems/edit/37023320`
  **Blocker Type**: auth
  **Access Needed**: authenticated Azure DevOps access to read the work item page, comments, linked tasks, and acceptance criteria

## Next Steps

1. Inspect the implementation of `SignalRService_Triage` and determine whether it uses provider configs, data stores, Key Vault, ACIS operations, declarative diagnostics, or an outdated ASPPartner SDK.
2. Map those implementation details to Steps 1 through 6 and identify the exact required subset.
3. Complete the required code or config updates.
4. Deploy the triage flow to Bleu and verify the deployment succeeds.
5. Run Bleu validation through ACIS actions and confirm diagnostics reliability.
6. Close work item `37023320` and confirm the S360 item is cleared before 2026-03-31.